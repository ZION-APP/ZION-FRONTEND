import 'package:flutter/material.dart';

class CalificacionRiesgos extends StatelessWidget {
  final String tipo;
  const CalificacionRiesgos(this.tipo, {Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
